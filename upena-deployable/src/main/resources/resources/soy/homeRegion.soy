{namespace soy.page}

/**
* @param wgetURL
* @param upenaClusterName
*/
{template .homeRegion}
    <div style="padding: 100px 100px 100px 100px;">
        <img src="/static/img/icon.png"></img>
    </div>
    <div class="panel panel-default">
        <div class="panel-heading"><img src="/static/img/icon.png" alt="Fun with deployment" width="28" height="28"><img>
                    &nbsp;About</div>
            
            <div class="panel-body">
                <pre>
                    Expand this cluster by running the following on a new node:<br><br>
                    wget -O upena.jar {$wgetURL} <br>
                    nohup java -jar upena.jar `hostname` {$upenaClusterName} &
                </pre>
            </div>
        </div>
    </div>
{/template}
