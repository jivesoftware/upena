#!/bin/bash

APP_ROOT=$(cd $(dirname $0)/..; pwd);
PORT=`grep InstanceConfig_default_managePort $APP_ROOT/config/instance.properties | sed -e 's/[^=]*=//'`

cd $APP_ROOT && curl localhost:${PORT}/manage/status
exit 0;